#include <iostream>
using namespace std;
int n, q, k, x;
const int N = 1e5+10;
int s[N], a[N];
int main()
{
    cin >> n >> q >> k >> x;
    for(int i=1;i<=n;i++) scanf("%d", &a[i]);
    for(int i=1;i<=n;i++) if(a[i] <)
    return 0;
}